# 项目跟进文档（面向实习简历展示）

## 0. 目标与定位

### 核心目标
把当前音乐 Agent 项目打造成一个**可讲清楚、可量化、可演示**的实习级项目，重点体现：

1. LangGraph 工作流编排能力
2. 工具定义与调用能力（Tooling）
3. Redis 热数据缓存/状态恢复能力
4. 本地冷数据记忆能力（`history.jsonl` + `soul.md` + `skill.md`）

### 一句话项目定位（后续可直接写简历）
基于 LangGraph 构建多节点音乐 Agent，采用 Redis + 本地文件的冷热分层记忆架构，实现会话可恢复、长对话低成本摘要、工具调用可观测与可审计。

---

## 1. 当前状态（你已有的基础）

你当前设计已经具备较好的雏形：

- Working Memory：`state["messages"]`
- Summary Memory：长对话触发摘要 + 保留滑动窗口
- Long-term Memory：本地 `md` 文件（用户偏好/人格）

这说明项目并不是从 0 开始，而是进入“工程化完善阶段”。

---

## 2. 总体架构升级（冷热分层）

## 2.1 热数据层（Redis）
- 用于会话状态恢复（LangGraph checkpointer）
- 用于高频工具结果缓存（如搜索结果、用户信息）
- 追求低时延和并发能力

## 2.2 温数据层（Summary）
- 当 token 超阈值时，触发摘要
- 保留最近 N 条消息 + 结构化摘要
- 目标是降低 token 成本并保持上下文准确度

## 2.3 冷数据层（本地文件）
- `history.jsonl`：追加式流水账、审计、复盘
- `user_profile.md`：用户偏好长期沉淀
- `soul.md`：系统风格与行为准则
- `skill.md`：工具使用经验与失败恢复策略

---

## 3. 分阶段执行计划（按优先级）

## Phase 1：工程骨架清晰化（建议 3~5 天）

### 任务 1：统一节点职责定义
- 输出一版工作流节点图（router/planner/tool_executor/memory_sync/finalizer）
- 明确每个节点输入字段、输出字段、失败分支

**验收标准**：
- 能用 3~5 分钟完整讲清整条链路

### 任务 2：统一日志事件结构
为每轮交互记录统一字段：
- `timestamp`
- `trace_id`
- `thread_id`
- `intent`
- `tool_calls`
- `latency_ms`
- `token_in/token_out`
- `task_status`
- `memory_update`

**验收标准**：
- `history.jsonl` 每条记录字段一致
- 可按字段检索复盘

### 任务 3：本地记忆文件治理规则
为 `user_profile.md` / `soul.md` / `skill.md` 设定更新规则：
- 触发条件
- 最大写入频率
- 冲突处理（新旧偏好冲突时）
- 回滚策略（错误更新恢复）

**验收标准**：
- 记忆写入有章法，不出现“模型胡写污染”

---

## Phase 2：Redis 工程化落地（建议 5~7 天）

### 任务 1：接入 Redis Checkpointer
- 将内存 checkpointer 替换为 Redis 持久化方案
- 通过 `thread_id` 验证跨进程恢复

**验收标准**：
- 服务重启后，同 `thread_id` 可恢复上下文

### 任务 2：设计缓存 Key 与 TTL 策略
建议命名规范：
- `session:{thread_id}:state`
- `cache:tool:{tool_name}:{query_hash}`

建议 TTL（可调整）：
- 会话状态：24h
- 工具缓存：10~30min

**验收标准**：
- 命中率可统计
- 过期策略清晰

### 任务 3：幂等保障与重复写防护
- finalizer 写日志时增加 `event_id`
- 重试时判断是否已写入，避免重复落盘

**验收标准**：
- 异常重试不产生重复流水

---

## Phase 3：摘要机制与质量控制（建议 3~5 天）

### 任务 1：摘要触发策略优化
- 结合 token 阈值 + 最近轮次双条件触发
- 保留“关键实体清单”（用户偏好、目标歌单、任务状态）

### 任务 2：摘要质量检查
每次摘要后打标签：
- 是否覆盖用户偏好
- 是否覆盖当前任务目标
- 是否覆盖未完成事项

### 任务 3：摘要前后指标对比
记录：
- token 压缩率
- 响应时延变化
- 回答一致性变化

**验收标准**：
- 有数据证明摘要不是“省 token 但掉智商”

---

## Phase 4：可观测与评测闭环（建议 3~5 天）

### 任务 1：建立小型评测集（10~20 条）
覆盖场景：
- 多轮偏好记忆
- 工具调用失败恢复
- 会话中断后恢复
- 长对话摘要后继续执行

### 任务 2：每次改动后跑回归
输出对比报告：
- 成功率
- 平均时延（P50/P95）
- token 成本
- 缓存命中率

### 任务 3：可观测面板（最小可用）
即使不用完整监控系统，也至少输出结构化日志 + 周报统计。

**验收标准**：
- 改动是否变好，有客观数据可看

---

## Phase 5：简历与 Demo 交付（建议 2~3 天）

### 任务 1：准备 3 条标准演示脚本
1. 冷启动新会话
2. 同 `thread_id` 中断恢复
3. 长对话触发摘要后继续任务

### 任务 2：README 升级
- 架构图（冷热分层）
- 模块说明（LangGraph 节点 + Tools）
- 指标表（优化前后对比）
- 快速启动（本地 + Redis）

### 任务 3：简历文案落地
确保每条都有“动作 + 技术 + 结果”。

---

## 4. 关键指标（简历必须可量化）

建议至少沉淀以下 6 项：

1. 会话恢复成功率（%）
2. 缓存命中率（%）
3. 平均响应时延 P50/P95（ms）
4. 单轮 token 消耗均值
5. 摘要触发后的 token 降幅（%）
6. 工具调用成功率与失败重试成功率（%）

---

## 5. 风险与应对

### 风险 1：本地记忆被错误更新
- **应对**：关键记忆更新加置信门槛 + 快照回滚

### 风险 2：Redis 缓存脏数据
- **应对**：TTL + 主动失效 + 按工具版本分 key

### 风险 3：summary 丢关键上下文
- **应对**：摘要模板中强制包含关键实体字段

### 风险 4：日志过多导致排查困难
- **应对**：统一 JSON 结构 + trace_id 贯穿全链路

---

## 6. 本周执行清单（可直接开工）

### Day 1
- 梳理节点职责图 + 字段流转表
- 定义 `history.jsonl` 统一 schema

### Day 2
- 接入 Redis checkpointer
- 完成 thread_id 恢复自测

### Day 3
- 增加工具缓存 key/TTL
- 记录命中率统计

### Day 4
- 完善 summary 触发策略与质量标签
- 增加摘要前后 token 对比记录

### Day 5
- 打通 3 条 demo 脚本
- 产出一版 README 指标表

---

## 7. 里程碑验收（阶段完成定义）

- **M1（骨架完成）**：架构图、日志 schema、记忆更新规则齐全
- **M2（可恢复）**：Redis 会话恢复稳定
- **M3（可量化）**：有至少 4 项优化指标可展示
- **M4（可演示）**：3 条 demo 可一次性跑通
- **M5（可求职）**：简历描述 + README + 演示视频/动图齐备

---

## 8. 后续我建议你优先做的两件事

1. 先完成 **Phase 1 + Phase 2**，因为这是“工程可信度”的核心。  
2. 完成后立刻沉淀一版“优化前后指标表”，这是你简历最有说服力的部分。


> 备注：你现在的基础已经不错，下一步重点不是“再堆功能”，而是把架构、指标、演示三件事做扎实。