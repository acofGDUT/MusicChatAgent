## 2026.5.15
### 开发功能：
1. 修改 `music_team_v3_1.py`：走音乐播放后直接输出。
2. 新增 local-chat 会话恢复能力（同线程历史回放）：
   - 前端 `music-agent-chat-ui/src/app/local-chat/page.tsx`：页面启动时优先读取 `localStorage`（`threadId + messages`）。
   - 若本地无消息，再按 `thread_id` 调后端拉历史并回填消息。
3. 新增后端历史接口 `GET /api/v1/chat/local/history`（`app/api/v1/endpoints.py`）：
   - 不接数据库，直接读取 `app/agents/memory/history.jsonl`。
   - 按 `thread_id` 过滤记录，从 `message_tail` 提取 human/ai 并映射为前端 `user/assistant` 消息。
4. 当前恢复逻辑总结：
   - 本地有历史：秒开恢复。
   - 本地无历史：后端兜底恢复同线程历史。
   - 都没有：进入空会话。
5. 新增 local-chat 节点 trace 能力（用于解决“中间节点有 JSON、最终回复无 JSON 导致前端不渲染”的问题）：
   - 后端 `app/api/v1/endpoints.py` 的 `POST /api/v1/chat/local` 改为基于 graph 更新流收集节点输出。
   - 返回结构新增 `data.trace`，每项包含：`node`、`content`、`is_json_like`。
   - 前端 `music-agent-chat-ui/src/app/local-chat/page.tsx` 保持主聊天区只显示最终 `reply`，并新增默认折叠的“过程面板/执行过程”。
   - 过程面板逻辑：逐节点展示输出；若节点输出是 JSON（`is_json_like=true`）可被识别并重点查看；非 JSON 仅作过程可视化。
6. 当前聊天渲染逻辑（5.15 版本）
   - 主渲染来源：最终 assistant `reply`（保证体验稳定）。
   - 可观测来源：`trace`（保证中间结构化结果不丢，便于排查和后续扩展时间线动画）。
7. local-chat 界面美化与交互增强（方案A，浅色）
   - UI 改为 70/30 工作台：左侧主聊天、右侧工具面板（Artifact 常驻区）。
   - 顶部改为吸附 Header，页面固定高度；滚动只发生在聊天区/面板区，不再整页滚动。
   - 消息视觉升级：用户/助手气泡分层，输入区统一为浅色现代风。
8. trace 展示增强（防止中间节点信息“看不见”）
   - assistant 消息下新增“执行过程”折叠区（默认收起），逐节点展示 `trace`。
   - 每节点显示 `node` + `JSON/TEXT` 标签；`is_json_like=true` 时尝试格式化 JSON 并支持一键复制。
   - 过滤系统噪声节点：前端隐藏 `memory_sync`、`finalizer`，减少干扰。
9. 逻辑总览（防遗忘）
   - 交互主线：用户消息 -> 后端 graph 流式收集节点 -> 返回 `reply + trace`。
   - 展示主线：主聊天区仅用 `reply`；调试/可观测走“执行过程(trace)”。
   - 结果：用户体验稳定（只看最终答复）+ 开发排查高可见（中间节点不丢）。
10. 歌曲封面与播放器返回结构打通（本次对话新增）
   - `app/services/music/song_service.py` 新增 `get_song_cover(song_mid, size=300)`：
     - 调 `client.song.get_detail(value=song_mid)`；
     - 用返回 `GetSongDetailResponse.track`（`track_info -> Song`）调用 `Song.cover_url(size)` 拿封面。
   - `app/api/v1/endpoints.py`：
     - 新增 `GET /song/cover?song_mid=...&size=...` 用于单独取封面；
     - 修改 `GET /song/play-url` 返回，新增 `cover` 字段，结构对齐前端 `PlayMusicPayload`（`type/url/title/artist/cover/description`）。
   - `app/tools/song_tools.py`：
     - `play_music_tool` 现在返回 JSON 字符串（`type=play_music`），并包含 `cover`；
     - 这样 Agent 侧工具输出可被 `music_player_artifact.tsx` 直接识别渲染播放器。
11. 本次核心逻辑（简版）
   - 播放链路：先拿 `url`（可播直链）+ 再拿 `cover`（详情页 `track.cover_url`）。
   - 返回规范：后端接口与工具统一按 `play_music` 载荷格式返回，前端无需额外适配。
### 已知限制：
1. history.jsonl 目前是 append-only，长期可能要做截断或分页