# 前端播放器渲染（Artifact 方案）

这份文档用于“换对话框也能续上上下文”：记录目前已完成的改造、当前架构决策，以及下一步如何继续改。

## 一、当前已完成改造（截至本次）

### 1) 单曲播放 Artifact（已完成）

文件：`agent-chat-ui/src/components/thread/messages/music-player-artifact.tsx`

已实现：
- `PlayMusicPayload` 类型（`type=play_music` + `url` 必填）
- `extractPlayMusicPayload`：解析纯 JSON / ```json 代码块
- `isPurePlayMusicPayloadText`：纯 payload 文本判定
- `MusicPlayerArtifact`：聊天区卡片 + 右侧 `<audio controls src=...>` 播放器

---

### 2) 歌单浏览 Artifact（已完成）

文件：`agent-chat-ui/src/components/thread/messages/playlist_browser_artifact.tsx`

已实现：
- `PlaylistBrowserPayload` 类型：
  - `type: "playlist_browser"`
  - `playlist_name`, `tracks`, `page`, `page_size`, `total_song_num`, `has_more`
- `extractPlaylistBrowserPayload`：解析纯 JSON / fenced JSON
- `isPurePlaylistBrowserPayloadText`：纯 payload 文本判定
- `PlaylistBrowserArtifact`：
  - 聊天区显示歌单卡片
  - 右侧显示歌曲列表
  - 支持“上一页/下一页”
  - 支持“点击某首歌请求播放”

> 当前实现中，分页和点播请求是通过向 Agent 继续发消息实现（`stream.submit`）。

---

### 3) AI 消息渲染链路接入（已完成）

文件：`agent-chat-ui/src/components/thread/messages/ai.tsx`

已实现：
- 同时识别并渲染：
  - `play_music` -> `MusicPlayerArtifact`
  - `playlist_browser` -> `PlaylistBrowserArtifact`
- 纯 payload 文本时隐藏 Markdown 原文，避免 JSON 直接暴露给用户

---

### 4) 后端 Agent 提示词扩展（已完成）

文件：`app/agents/music_team_v2_2.py`

已实现：
- `PlayAgent` 支持两种输出：
  - 单曲：`type=play_music`
  - 歌单页：`type=playlist_browser`
- 已加分页策略要求（默认 `num=10`，支持下一页）
- 接入工具：`get_playlist_detail_tool`

---

## 二、当前痛点（你刚提出）

> 渲染后，点击“播放/下一页”不应再走 Agent 对话，否则一轮太慢。

结论：完全正确。下一步应改成“前端直连后端 API”，跳过 LLM 对话回路。

---

## 三、下一步目标架构（推荐）

### 目标

- **首次**可以由 Agent 返回 `playlist_browser`（也可后续改为纯 API 驱动）
- Artifact 渲染后：
  - 点歌 -> 直接调后端函数接口拿 `url`
  - 翻页 -> 直接调后端分页接口
- 不再调用 `stream.submit` 发对话消息

### 推荐新增后端接口

1. `GET /api/v1/playlist/{dirid}/tracks?page=1&page_size=10`
- 返回：`playlist_browser` payload

2. `GET /api/v1/song/play-url?song_mid=xxx`
- 返回：`play_music` payload（至少包含 `url`）

---

## 四、前端待改点（Checklist）

目标文件：`agent-chat-ui/src/components/thread/messages/playlist_browser_artifact.tsx`

把以下逻辑从“对话请求”改成“HTTP 请求”：

1) `requestPage(targetPage)`
- 现状：`stream.submit("请加载第N页...")`
- 目标：`fetch('/api/v1/playlist/...')`，然后 `setPayload(...)`

2) `requestSongPlay(track)`
- 现状：`stream.submit("请播放这首歌...")`
- 目标：`fetch('/api/v1/song/play-url?song_mid=...')`，然后更新本地 `audioUrl/currentTrack`

3) UI 增加状态
- `isPageLoading`
- `isPlayLoading`
- `error`

4) 组件内本地播放器
- 在 `PlaylistBrowserArtifact` 内部增加 `<audio>`，用于点击即播
- 不依赖新 AI 消息才能播放

---

## 五、后端待改点（Checklist）

建议新增 FastAPI 路由文件，例如：
- `app/api/v1/endpoints/player.py`（可按你项目结构调整）

接口职责：
1) 歌单分页接口：内部复用你已有的 `get_playlist_detail` 服务逻辑
2) 播放链接接口：内部复用 `song_service.get_playable_url(song_mid)`

注意：
- 做参数校验（页码、page_size 上限、song_mid 非空）
- 错误统一返回结构（便于前端 toast）

---

## 六、稳定的数据协议（建议固定）

### A. 歌单分页响应

```json
{
  "type": "playlist_browser",
  "playlist_name": "通勤歌单",
  "dirid": 123,
  "page": 2,
  "page_size": 10,
  "total_song_num": 86,
  "has_more": true,
  "tracks": [
    { "index": 11, "song_mid": "001abc", "title": "晴天", "artist": "周杰伦", "cover": "" }
  ]
}
```

### B. 单曲播放响应

```json
{
  "type": "play_music",
  "song_mid": "001abc",
  "title": "晴天",
  "artist": "周杰伦",
  "url": "https://..."
}
```

---

## 七、继续改写时可直接使用的提示词

### 模板 1：把 Artifact 交互改为直连 API（核心模板）

```text
请把 `agent-chat-ui/src/components/thread/messages/playlist_browser_artifact.tsx` 从“通过 stream.submit 触发 Agent 对话”改为“前端直接调用后端 API”。

目标：
1) 点击“上一页/下一页”时调用 GET /api/v1/playlist/{dirid}/tracks?page=&page_size=
2) 点击“播放”时调用 GET /api/v1/song/play-url?song_mid=
3) 在组件内部维护播放器状态（currentTrack/audioUrl/loading/error）
4) 保持现有 Artifact UI 结构不变（聊天卡片 + 右侧面板）
5) 不影响现有 play_music / playlist_browser 的消息解析逻辑

输出要求：
- 直接修改现有文件
- 使用 TypeScript 严格类型
- 增加必要的错误处理和 loading 状态
```

### 模板 2：新增后端直连接口

```text
请在后端新增两个 API：
1) GET /api/v1/playlist/{dirid}/tracks?page=1&page_size=10
2) GET /api/v1/song/play-url?song_mid=xxx

要求：
- 复用现有服务层能力（歌单详情与播放链接）
- 返回结构与前端协议一致（playlist_browser / play_music）
- 参数校验完整（page>=1, page_size<=50, song_mid 非空）
- 错误返回统一 JSON（message/code）
```

### 模板 3：端到端联调

```text
请完成“歌单列表 -> 点击播放 -> 分页切换”的端到端联调：
- 前端 Artifact 改为直连 API
- 后端补齐 API
- 保持 Agent 仅用于首次理解用户请求并返回首屏 playlist_browser
- 后续交互全部走 API，不新增聊天消息

请输出：
1) 修改的文件列表
2) 每个文件的关键改动
3) 如何手工验证（3~5 步）
```

---

## 八、当前项目里与你功能相关的关键文件

- `app/agents/music_team_v2_2.py`
- `app/tools/playlist_tools.py`
- `app/tools/song_tools.py`
- `agent-chat-ui/src/components/thread/messages/ai.tsx`
- `agent-chat-ui/src/components/thread/messages/music-player-artifact.tsx`
- `agent-chat-ui/src/components/thread/messages/playlist_browser_artifact.tsx`

---

## 九、你下个对话可直接复制的开场语

```text
请按《前端播放器渲染.md》里的“模板1 + 模板2”直接落地改造：
先实现后端两个直连 API，再把 playlist_browser_artifact 改成点击播放/翻页走 API，不再走 Agent 对话。
```
